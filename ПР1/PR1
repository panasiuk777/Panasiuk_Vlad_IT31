Сторення:
CREATE TABLE movies (
    id INTEGER PRIMARY KEY,
    title TEXT,
    genre TEXT,
    duration_min INTEGER,
    year INTEGER,
    age_rating INTEGER
);
додавання даних:
INSERT INTO movies (title, genre, duration_min, year, age_rating) VALUES
('Інтерстеллар', 'наукова фантастика', 169, 2014, 12),
('Початок', 'фантастика', 148, 2010, 12),
('Титанік', 'драма', 195, 1997, 12),
('Один вдома', 'комедія', 103, 1990, 0),
('Матриця', 'наукова фантастика', 136, 1999, 16),
('Король Лев', 'анімація', 88, 1994, 0);
перевірка:
SELECT * FROM movies;
перевірка структури таблиці:
PRAGMA table_info(movies);